<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define the path to the public Downloads directory -->
    <external-path
        name="external_downloads"
        path="Download/" />
    <!-- This path corresponds to Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOWNLOADS) -->
    <!-- Ensure the path attribute matches where the APKs are saved -->
</paths>
